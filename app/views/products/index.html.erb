<div class="page-header">
  <h1>Products</h1>
</div>

<% if user_signed_in? %>
  <div class="pull-right">
    <%= link_to "New", new_product_path, class: 'btn btn-primary' %>
  </div>
<% end %>

<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Price</th>
      <th>Status</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @products.each do |product| %>

      <tr>
        <td><%= link_to product.id, product_path(product) %></td>
        <td><%= product.name %></td>
        <td><%= number_to_currency(product.price, precision: 2) %></td>
        <td><%= product.status%></td>
        <td>
          <% if user_signed_in? %>
            <%= link_to "Edit", edit_product_path(product), class: 'btn btn-default btn-xs' %>
            <%= link_to "Eliminar",
              product_path(product),
              method: :delete,
              data: { confirm:  "Are you sure?"},
              class: 'btn btn-xs btn-danger' %>
            <%= link_to "Archive product", archive_product_path(product), method: :put%>

              </div>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
